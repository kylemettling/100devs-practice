// shift removes the first element from an array and returns the removed element.

// this method changes the length of the array.

// shift takes no parameters.

// 1. Remove leading element
const arr = [2, 3, 1, 2];
const first = arr.shift();
