// match retrieves the result of matching a string against a regular expression. Returns an array of those values that match
// Time Complexity: O(N) where N is the size of the string
// const p = 'The quick brown fox jumps over the lazy dog. It barked.';
// const regex = /[A-Z]/g;
// p.match(regex); => returns [“T”,”I”]
